# PocketGPT web v6.1 — cancellation UI hotfix

- Applies the cancellation response immediately in the UI.
- Refreshes server state after cancellation.
- Shows `APPROVAL_PENDING` as “Not completed” instead of “Scheduled”.
